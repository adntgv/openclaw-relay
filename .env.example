# Copy to .env and fill in values for docker-compose
ADMIN_TOKEN=dev-admin-token

# Relay client config
RELAY_URL=ws://relay-server:8080/ws
RELAY_TOKEN=replace-with-token
CLAW_ID=local-client
CAPABILITIES=shell
CLIENT_VERSION=0.1.0
